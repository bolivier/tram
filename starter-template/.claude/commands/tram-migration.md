---
name: tram-migration
description: Generate `.up.sql` and `.down.sql` migration files through a short conversation. 
---

Run the `create-migration` skill and follow it.

The skill owns the conversation, the SQLite dialect, and the SQL templates, so
migration advice has one home rather than two copies to keep in agreement.
